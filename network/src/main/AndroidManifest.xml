<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".provider.NetworkInterceptorContentProvider"
            android:authorities="${applicationId}.provider.NetworkInterceptorContentProvider"
            android:exported="false" />

    </application>

</manifest>